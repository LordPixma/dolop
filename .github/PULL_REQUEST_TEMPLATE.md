## What & why

<!-- What does this change, and what problem does it solve? -->

## Checklist

- [ ] `npm run typecheck` and `npm test` pass
- [ ] Tests added/updated for pure logic where practical
- [ ] Migration-fidelity changes reflected in `docs/bittitan-parity.md`
- [ ] D1 schema changes are a new file in `migrations/`
- [ ] No secrets in code, config, or fixtures
